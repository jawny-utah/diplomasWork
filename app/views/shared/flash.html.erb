<% if flash.present? %>
  <div class="flex flex-col border border-solid border-red-500 bg-red-200 p-3 rounded-lg">
    <% flash.each do |msg| %>
      <div><%= msg %></div>
    <% end %>
    <% flash.clear %>
  </div>
<% end %>
